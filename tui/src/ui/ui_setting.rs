use ratatui::{Frame, layout::Rect};

use crate::data::{AppData, Settings};

pub fn ui_setting_render(
    frame: &mut Frame,
    area: Rect,
    app_data: &mut AppData,
    setting: &mut Settings,
) {
}
